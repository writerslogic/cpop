# /etc/tmpfiles.d/cpop.conf
# tmpfiles.d configuration for cpop

# Create runtime directory
d /run/cpop 0750 cpop cpop -

# Create data directory
d /var/lib/cpop 0750 cpop cpop -

# Create log directory
d /var/log/cpop 0750 cpop cpop -

# Create config directory
d /etc/cpop 0750 root cpop -
