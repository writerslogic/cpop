# linuxdeploy configuration for CPOP AppImage
# https://github.com/linuxdeploy/linuxdeploy

appimage:
  name: writerslogic
  version: "1.0.0"
  arch: x86_64
  update-information: gh-releases-zsync|writerslogic|writerslogic|latest|writerslogic-*x86_64.AppImage.zsync

# Files to include
files:
  - source: usr/bin/cpop
    destination: usr/bin/cpop
  - source: usr/bin/witnessctl
    destination: usr/bin/witnessctl
  - source: usr/bin/cpop-ibus
    destination: usr/bin/cpop-ibus
  - source: usr/share/applications/cpop.desktop
    destination: usr/share/applications/cpop.desktop
  - source: usr/share/icons/hicolor/256x256/apps/cpop.png
    destination: usr/share/icons/hicolor/256x256/apps/cpop.png
  - source: usr/share/metainfo/cpop.appdata.xml
    destination: usr/share/metainfo/cpop.appdata.xml

# Runtime configuration
runtime:
  # Embed update information for AppImageUpdate
  update-information: true

# Desktop integration
desktop-integration:
  enable: true

# Environment variables
environment:
  WRITERSLOGIC_APPIMAGE: "1"
